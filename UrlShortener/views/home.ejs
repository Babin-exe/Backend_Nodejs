<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        body {
            font-family: sans-serifƒ;
        }
    </style>
</head>

<body>
    <h1>Url Shortener</h1>

    <% if (locals.id) { %>
        <p>
            Short url Generated : http://localhost:4000/api/url/<%= id %>
        </p>
        <% } %>

            <div>
                <form method="POST" action="/api/url">
                    <label for="text">Enter your url :</label>
                    <input id="text" type="text" placeholder="https://hehe.com" name="url">
                    <button type="submit">Generate Url</button>
                </form>
            </div>
            <% if (locals.urls) { %>



                <div>
                    <table>
                        <thead>
                            <th>S-No</th>
                            <th>ShortUrl</th>
                            <th>LongUrl</th>
                            <th>Clicks</th>
                        </thead>
                        <tbody>
                            <% urls.forEach((url ,index)=> { %>
                                <tr>
                                    <th>
                                        <%= index+1 %>
                                    </th>
                                    <th>
                                        <%= url.shortId %>
                                    </th>
                                    <th>
                                        <%= url.redirectUrl %>
                                    </th>
                                    <th>
                                        <%= url.visitHistory.length %>
                                    </th>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </div>
                <% } %>
</body>

</html>